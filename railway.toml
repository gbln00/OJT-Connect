[build]
builder = "nixpacks"

[deploy]
releaseCommand = "php artisan migrate --force && php artisan storage:link && php artisan optimize:clear"
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"