[build]
builder = "nixpacks"

[deploy]
releaseCommand = "php artisan optimize:clear && php artisan config:clear && php artisan migrate --force"

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"